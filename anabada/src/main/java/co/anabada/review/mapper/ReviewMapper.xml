<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.anabada.review.mapper.ReviewMapper">

	<select id="reviewList" resultType="review">
		Select r.review_num
				,r.review_score
				,r.review_date
				,r.review_comment
				,o.order_id
				,m.member_name
		FROM review r
		JOIN anvd_order o ON r.order_id = o.order_id
		JOIN member m ON o.order_id = m.member_num
	</select>


</mapper>